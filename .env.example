NEXT_PUBLIC_API_BASE_URL=https://nestino-backend-production.up.railway.app/api/v1
# Optional server-side proxy target (defaults to NEXT_PUBLIC_API_BASE_URL). Browser calls /api/v1 on the same host.
# API_PROXY_TARGET=https://nestino-backend-production.up.railway.app/api/v1
